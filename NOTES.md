# NOTES.md

## Preferensi belajar (mahasiswa)
- Bahasa percakapan: Bahasa Indonesia (gue jelasinnya santai, ngikutin vibe dia).
- Pengin dapet jawaban yang bermanfaat + no bs.
- Ikutin materi dosen berurutan (modul 1 → 2 → …). Kalo sudah, lanjut; gitu terus.
- Misi: kuliah + bikin app sendiri (app anemia). Jadikan setiap modul nyambung ke misi ini.
- Suasana obrolan santai ("bro", "gue"), bukan formal.

## Progress kursus
- [x] Modul 1: Pengenalan Flutter & Dart — teori dibaca, env sudah siap, contoh kode (hello_dart + main.dart "Halo Mahasiswa") sudah diterapkan & diuji.
- [x] Modul 2: Instalasi & Konfigurasi — verifikasi: Flutter SDK ✓, PATH ✓, Android Studio ✓, Android SDK + cmdline-tools + licenses ✓, emulator ✓, VS Code + ekstensi (Flutter&Dart, Awesome Snippets, Error Lens) ✓, `flutter devices` ✓. Plus: `flutter run` beneran ke emulator Android (lihat catatan emulator di bawah).
- [x] Modul 3: Variabel & Tipe Data — teori dibaca, praktik `bin/variabel_anemia.dart` (semua topik: var/eksplisit/final/const, int/double/num/String/bool/dynamic, string interpolation, List/Map/Set, null safety, konversi tipe) jalan + `flutter analyze` No issues. Lesson 003 dibuat & dibuka.
- [x] Modul 4: Function & OOP — teori dibaca, praktik `bin/fungsi_oop_anemia.dart` (function + arrow, named/optional params, class Pasien + getter + encapsulation, inheritance & polymorphism, abstract class, mixin) jalan + `flutter analyze` No issues. Lesson 004 dibuat & dibuka.
- [x] Modul 5: Collection & Async — teori dibaca, praktik `bin/collection_async_anemia.dart` (List/Set/Map + union/intersection, spread + collection if/for, map/where/any/every/reduce, Future + async/await + try-catch-finally, Stream async*/yield) jalan + `flutter analyze` No issues. Lesson 005 dibuat & dibuka.
- [x] Modul 6: Widget Dasar — teori dibaca, `lib/main.dart` di-upgrade jadi "Kartu Pasien Anemia" (Scaffold/AppBar, Text/TextStyle, Container+BoxDecoration, Row/Column+SizedBox, Icon, Image.network+errorBuilder, Elevated/Outlined/Icon button). `flutter test` pass + `flutter analyze` No issues. Lesson 006 dibuat & dibuka. NOTE: deviasi dari dosen — dosen bikin project baru/modul, kita upgrade SATU project (misi: app anemia tumbuh terus).
- [x] Modul 7: Layout & Responsive — teori dibaca, `lib/main.dart` di-upgrade jadi "Dashboard Anemia" (SafeArea, LayoutBuilder 2↔4 kolom, ListView.builder + GridView.count shrinkWrap, Stack+Positioned badge status, Expanded 3 kartu statistik, MediaQuery → SnackBar ukuran layar). Logika anemia dari M5 kepake (where count). `flutter test` (cek ANEMIA=3 dari 6 pasien) pass + `flutter analyze` No issues. Lesson 007 dibuat & dibuka.
- [x] Modul 8: Navigation & Routing — teori dibaca, app jadi multi-halaman: struktur file dipecah (`lib/screens/`), `main.dart` = AnemiaApp + named routes + HomeShell (NavigationBar Beranda/Jadwal/Info), dashboard punya Drawer (header + Tentang Aplikasi via pushNamed '/tentang'), daftar pasien bisa diklik → DetailPasienPage (passing data via constructor) → tombol "Catat ke Jadwal" nge-pop balikin nama → SnackBar di dashboard (await + context.mounted — pola return data dari M5 async). Tantangan dosen (Katalog Produk) diadaptasi jadi "Katalog Pasien". 3 widget test pass (dashboard, flow navigasi lengkap, drawer+named route) + `flutter analyze` No issues. Lesson 008 dibuat & dibuka.
- [x] Modul 9: State Management — teori dibaca, `provider` 6.1.5 ditambah. TodoList dosen diadaptasi jadi "Jadwal Pemantauan": `lib/models/jadwal_model.dart` (ChangeNotifier + getter total/jumlahSelesai/Belum/persentase, tolak duplikat), `main.dart` bungkus ChangeNotifierProvider + tab Jadwal jadi konsumen (CheckboxListTile toggle, hapus, Hapus Semua Selesai, counter AppBar), dashboard "Catat ke Jadwal" sekarang ngesimpan ke state (bukan cuma SnackBar), `lib/screens/statistik_screen.dart` baru baca provider SAMA (route /statistik). 5 test pass (1 unit test model + 4 widget test) + analyze No issues. Lesson 009 dibuat & dibuka.
- [x] Modul 10: REST API & HTTP — teori dibaca, `http` 1.6.0 ditambah + INTERNET permission di AndroidManifest utama. Bonus dosen (/users JSONPlaceholder) diangkat jadi praktik utama: tab baru "Direktori" di NavigationBar, `lib/models/user_pasien.dart` (fromJson), `lib/services/pasien_service.dart` (GET+POST, client injectable), `lib/screens/direktori_screen.dart` (FutureBuilder 3 keadaan + RefreshIndicator + error state), `lib/screens/detail_direktori_screen.dart` (detail page + form tambah POST dengan SnackBar hijau/merah). Ketemu & fix bug nyata: setState arrow return Future → harus block body. 10 test pass (3 unit service pake MockClient offline + 2 widget direktori + 5 lama) + analyze No issues — pakai `flutter test --concurrency=1` karena RAM 7.7GB (4 file test paralel = OOM). Lesson 010 dibuat & dibuka.
- [x] Modul 11: Local Storage (SQLite) — teori dibaca, `sqflite` 2.4.4 + `shared_preferences` 2.5.5 + `path` ditambah. Gap M9 tutup: jadwal pemantauan persist ke DB lokal. `lib/models/jadwal_item.dart` baru (JadwalItem + id + dibuatAt, toMap/fromMap), `lib/services/jadwal_store.dart` (interface `JadwalStore` + `JadwalDbStore` singleton SQLite — pola DatabaseHelper dosen), `JadwalModel` refactor besar (load pas init via `_init = _muat()`, semua operasi async by-id, getter `siap` + `init`), `AnemiaApp` + param `jadwalStore` (injectable), HomeShell simpan/pulihkan **tab terakhir** (bonus dosen "login persist" diadaptasi — kita nggak punya login), JadwalPage spinner + toggle/hapus by-id. Widget test semua inject `InMemoryJadwalStore` (test/fakes/) + `SharedPreferences.setMockInitialValues` — pelajaran DI: sqflite butuh plugin yang nggak ada di test. 12 test pass (2 unit model incl. test persisten-restart + 3 service + 2 direktori + 5 widget) + analyze No issues. Lesson 011 dibuat & dibuka.
- [x] Modul 12: Pengelolaan Media (Image, Audio, File) — teori dibaca, `image_picker` 1.2.3 + `path_provider` 2.1.6 ditambah (file_picker & audioplayers = teori dulu, dicatat jujur). Stub "Input Lab" di dashboard akhirnya diwujudkan: tile-nya sekarang nuduh ke **Galeri Hasil Lab** (`lib/screens/galeri_screen.dart`) — ambil foto kamera/galeri (imageQuality 80, maxWidth 1280), simpan PERMANEN ke direktori dokumen (`lib/services/galeri_service.dart`, getDir injectable, nama `foto_<timestampMs>.jpg`), grid 3 kolom + overlay tanggal (tantangan 2), long-press → AlertDialog konfirmasi → delete + reload + SnackBar (tantangan 1), empty state, errorBuilder buat foto korup, preview dialog. Audio recorder & share_plus di-skip (nggak nyambung misi — dicatat). Manifest + `READ_MEDIA_IMAGES` (deviasi: image_picker pakai intent sistem, CAMERA/storage permission nggak wajib; pasang tak terpakai = lint warning release). **Pelajaran test terbesar:** IO asli (file/gambar) di widget test = zona FakeAsync → butuh alternasi `pumpAndSettle` ↔ `tester.runAsync` berulang (helper `settleDenganIO`), seeding sync biar zone-proof, plus teardown retry (lock Windows + scan Defender). 18 test pass (3 unit galeri service + 3 widget galeri + 12 lama) + analyze No issues. Lesson 012 dibuat & dibuka.
- [x] Modul 13: Testing Aplikasi — teori dibaca; project udah punya 18 test, jadi modul ini ngerapiin & ngedalemin. **Tantangan 1** (Kalkulator) diadaptasi → `lib/models/anemia_logic.dart` (class PURE: statusAnemia, tingkatKeparahan enum, persentaseAnemia, deltaHb; batas 12.0 g/dL sekarang satu sumber kebenaran — dashboard refactor ke `AnemiaLogic.batasHbNormal`). 12 unit test group + matcher + edge case (boundary 12.0/8.0/7.9, list kosong, closeTo). **Tantangan 2** (ProfileCard nama+email) → `lib/widgets/profile_card.dart`, dipasang beneran di header detail Direktori (inisial avatar, subtitle UID) + 3 widget test. **Tantangan 3** (widget test tambah/toggle/hapus) → `test/jadwal_widget_test.dart` (CheckboxListTile toggle, delete descendant per-baris, Hapus Semua Selesai). +1 test validasi form kosong di direktori (materi "form nolak email kosong"). **Integration test** dibikin (`integration_test/app_test.dart`, dev_dependency `integration_test` sdk:flutter — pub add biasa gagal: salah ambil versi pub.dev non-null-safe; idempoten, nggak sentuh API biar mandiri; jalan `flutter test integration_test/app_test.dart` — butuh emulator, belum dijalankan sesi ini karena RAM). Deviasi: mockito+build_runner dosen → MockClient package:http/testing (hasil sama, tanpa codegen). **Coverage: 86.6% baris lib/ (568/656, 16 file)** dihitung manual dari lcov.info (genhtml nggak ada). 35 test pass + analyze No issues. Lesson 013 dibuat & dibuka.
- [x] Modul 14: Build & Release — teori dibaca; project (bukan project terpisah, misal flutter_widget_dasar — keputusan awal) dikasih identitas rilis: **nama** "Anemia Care" (AndroidManifest label, sebelumnya "anemia" lowercase), **applicationId** `id.anemiacare.app` + versionCode/versionName otomatis dari pubspec (1.0.0+1). **Ikon & splash DIGENERATE programmatic** (`tool/gen_icon.dart` — droplet merah di gradient pink, PNG encoder murni pakai zlib dart:io + CRC32 manual + supersampling 2×2 AA; bug nyata: CRC ditulis sebelum data → PNG korup → fix urutan chunk) → `dart run flutter_launcher_icons` (mipmap semua density + adaptive v26) & `flutter_native_splash:create` (drawable + styles incl. v31 Android 12+). **Keystore upload** (`keytool` RSA 2048 valid 10000 hari, `android/app/upload-keystore.jks`) + `android/key.properties` (gitignore proteksi `android/key.properties` & `android/app/*.jks`) + signingConfig release di build.gradle.kts dengan fallback debug key kalau file belum ada. **Build sukses di background (RAM cukup!):** debug APK 150.72MB (206s), release APK 50.4MB (476s, + font tree-shaking 99.7%: MaterialIcons 1.6MB→5.7KB), terverifikasi personal: aapt `package: name='id.anemiacare.app' versionCode='1' versionName='1.0.0'` + label 'Anemia Care', apksigner cert `CN=Anemia Care, OU=Mobile Programming...` (keystore upload, BUKAN debug). **AAB** `flutter build appbundle --release` → `app-release.aab` **49.4MB** (109s, cache hangat). **Split per-ABI** `--release --split-per-abi` → arm64 **17.8MB**, armeabi-v7a 15.3MB, x86_64 19.2MB (184s) — universal 50.4MB vs arm64 17.8MB = ≈65% lebih kecil. .gitignore dll. Tantangan 1-3 ✅, 4 (install HP) & 5 (obfuscate; minify release udah default) → sesi user. Lesson 014 dibuat.
- [ ] Modul 15: Publish ke Play Store

## Environment
- Flutter 3.47.5 stable di C:\Users\AcerAG14\flutter (PATH persistent aktif di terminal baru)
- Dart 3.13.4
- Project: C:\Users\AcerAG14\Documents\Project\Android\Anemia (nama package `anemia`)
- Android SDK: cmdline-tools + licenses sekarang OK (flutter doctor: Android toolchain √)
- VS Build Tools 2019 incomplete — hanya pengaruh ke `flutter run -d windows`.
- **Emulator utama: `Anemia_Phone`** (system image android-34 google_apis, RAM 1536-2048MB) — dibuat karena `Medium_Phone` (android-37.1 PlayStore + ps16k) kelebihan beban buat RAM 7.7 GB (crash saat boot). Launch: `emulator -avd Anemia_Phone` atau `flutter emulators --launch Anemia_Phone`.
- **Fix penting:** `android/gradle.properties` template Flutter pakai `-Xmx8G` → JVM crash OOM di mesin RAM 7.7 GB (crash log: "insufficient memory"). Sudah diturunkan ke `-Xmx1536m -XX:MaxMetaspaceSize=512m` + `kotlin.daemon.jvmargs=-Xmx1024m`. Build Android pertama juga butuh download NDK + Build-Tools 36 (sudah terpasang otomatis).
- **Tips RAM:** tutup Antigravity IDE / browser saat pakai emulator, biar nggak kena thrashing (koneksi `flutter run` putus, app tetap jalan).
- **Test wajib pakai `flutter test --concurrency=1`** — 4+ file test paralel = VM Dart crash OOM (7.7GB). Sekarang ada 35 test (+ coverage 86.6% lib/).
- **Developer Mode (Windows)** — `flutter pub add` warning "Building with plugins requires symlink support". Teruji: build Android TIDAK kenal error symlink (gagal cuma karena RAM). Kalau nanti mau build desktop/web, aktifin Developer Mode (Settings → Privacy & Security → For developers) atau jalanin terminal sebagai admin.